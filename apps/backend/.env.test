# apps/backend — Integration-Tests. medusaIntegrationTestRunner legt eine
# eigene Test-DB ueber DB_*-Variablen an (NICHT DATABASE_URL — siehe
# @medusajs/test-utils/dist/database.js).

DB_HOST=localhost
DB_PORT=15432
DB_USERNAME=medusa
DB_PASSWORD=medusa

REDIS_URL=redis://localhost:16379

JWT_SECRET=supersecret-dev-only
COOKIE_SECRET=supersecret-dev-only
